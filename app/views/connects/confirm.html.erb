<h3>Make sure of your tweet before submitting.</h3>

<%= form_with(model: @connect, local: true) do |form| %>
  <%= form.hidden_field :content %>

  <p>content:<%= @connect.content %></p>
  <%= form.submit  "Submit" %>

  <%= form.submit "Back", name: 'back' %>

<% end %>
